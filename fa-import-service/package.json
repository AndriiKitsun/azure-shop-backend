{
  "name": "fa-import-service",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "azurite": "azurite-blob -s -l ./azurite",
    "start:dev": "npm run build && func start",
    "start": "run-p azurite start:dev",
    "deploy": "sh scripts/deploy.sh"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/storage-blob": "^12.17.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "azurite": "^3.30.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  }
}
